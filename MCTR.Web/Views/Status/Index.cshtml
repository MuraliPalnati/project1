@model IEnumerable<MCTR.DomainEntity.Status>

@{
    ViewBag.Title = "MCTR STATUS ID";
}

<h2>Status IDs</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.STATUS_ID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.STATUS_DESCR)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.TextBoxFor(modelItem=>item.STATUS_ID)
            @*@Html.DisplayFor(modelItem => item.STATUS_ID)*@
        </td>
        <td>
            @Html.TextBoxFor(modelItem => item.STATUS_DESCR)
            @*@Html.DisplayFor(modelItem => item.STATUS_DESCR)*@
        </td>
        <td>
           @Html.ActionLink("Edit", "Edit", item) |
            @*@Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |*@
            @Html.ActionLink("Delete", "Delete", item)
        </td>
    </tr>
}

</table>
